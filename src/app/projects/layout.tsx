import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Projects by Kumail Kmr | GitHub: kumailkmr",
};

export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
